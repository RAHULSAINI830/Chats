{
  "name": "realtime-chat-backend",
  "version": "1.0.0",
  "description": "Express & Socket.IO backend for realtime chat with file uploads",
  "main": "server.js",
  "scripts": {
    "build": "cd realtime-chat-frontend && npm install && npm run build && cd .. && npm install",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mongoose": "^8.13.2",
    "multer": "^1.4.5-lts.2",
    "socket.io": "^4.8.1",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
